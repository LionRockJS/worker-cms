<div class="px-4 py-5 sm:px-6 sm:py-8 lg:px-8">
  <div class="mx-auto max-w-3xl rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
    <div class="flex items-start gap-4">
      <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-indigo-50 text-indigo-600">
        <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="{{ iconHrefPrefix }}#lock-closed"></use></svg>
      </div>
      <div>
        <h1 class="text-2xl font-bold text-gray-900">{{ "view_strings.viewer_home.heading" | t }}</h1>
        <p class="mt-2 text-sm text-gray-600">{{ "view_strings.viewer_home.message" | t }}</p>
        <p class="mt-2 text-sm text-gray-500">{{ "view_strings.viewer_home.apps_hint" | t }}</p>
      </div>
    </div>
  </div>
</div>
